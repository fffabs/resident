# macOS
.DS_Store

# Claude Code session
.claude/

# Local Claude skills cache (not project files)
skills/

# Commercial fonts — ship only the woff/woff2 we actually use,
# keep the redistributable formats (eot/ttf/svg) local.
assets/fonts/**/*.eot
assets/fonts/**/*.ttf
assets/fonts/**/*.svg
