# Loops API key — used by /api/subscribe to add new signups to the "Prelaunch"
# audience. Get a key at https://loops.so/ → Settings → API.
# In Vercel: Project → Settings → Environment Variables → add LOOPS_API_KEY
# for Production (and Preview if you want previews to work too).
LOOPS_API_KEY=
